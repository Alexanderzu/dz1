mixin icon(name,mod)
    - mod = mod || ''
    svg(class="icon icon-" + name + ' ' + mod)
        use(xlink:href="assets/img/sprite.svg#" + name)
        
        
mixin link(href, name)
    a(href=href)&attributes(attributes)= name
    
// +link('/foo', 'foo')(class="bt")